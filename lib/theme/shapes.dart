// Flutter imports:
import 'package:flutter/material.dart';

final RoundedRectangleBorder kCardShape = RoundedRectangleBorder(
  borderRadius: BorderRadius.circular(5.0),
  side: BorderSide(
    width: 1.0,
    color: Colors.cyan.shade600,
  ),
);


final RoundedRectangleBorder kCardAdvisor1 = RoundedRectangleBorder(
  borderRadius: BorderRadius.circular(25.0),
  side: BorderSide(
    width: 2.0,
    color: Colors.cyan.shade600,
  ),
);


final RoundedRectangleBorder kCardAdvisor2 = RoundedRectangleBorder(
  borderRadius: BorderRadius.circular(15.0),
);


final RoundedRectangleBorder kShapeDettVend = RoundedRectangleBorder(
  borderRadius: BorderRadius.circular(15.0),
  side: BorderSide(
    width: 1.0,
    color: Colors.cyan.shade600,
    style: BorderStyle.solid,

  ),
);


final RoundedRectangleBorder kShapeOrdineProv = RoundedRectangleBorder(
  borderRadius: BorderRadius.circular(10.0),
  side: BorderSide(
    width: 2.0,
    color: Colors.cyan.shade600,
  ),
);


final RoundedRectangleBorder kShapeSchedaProd = RoundedRectangleBorder(
  borderRadius: BorderRadius.circular(2.0),
  side: BorderSide(
    width: 1.0,
    color: Colors.cyan.shade600,
  ),
);


final RoundedRectangleBorder kShapeAlertDialog = RoundedRectangleBorder(
  side: BorderSide(width: 3.0, color: Colors.cyan.shade600),
  borderRadius: BorderRadius.circular(10.0),
);


final BoxDecoration kBoxDec = BoxDecoration(
  borderRadius: BorderRadius.circular(10.0),
  border: Border.all(
    width: 1.0,
    color: Colors.cyan.shade600,
  ),
);


final BoxDecoration kBoxDecV2 = BoxDecoration(
  borderRadius: BorderRadius.circular(15.0),
  border: Border.all(
    width: 1.0,
    color: Colors.cyan.shade600,
  ),
);

final OutlineInputBorder kTextFieldShape = OutlineInputBorder(
  borderRadius: BorderRadius.circular(10.0),
  borderSide: BorderSide(
    width: 1.0,
    color: Colors.cyan.shade600,
  ),
);